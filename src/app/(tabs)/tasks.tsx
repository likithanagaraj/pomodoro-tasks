import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Tasks = () => {
  return (
    <SafeAreaView className='bg-background flex-1 px-6 py-2'>
      <Text >tasks</Text>
    </SafeAreaView>
  )
}

export default Tasks